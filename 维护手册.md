# TAGMA 小组网站 · 日常维护手册

本站内容全部由 `contents/` 目录下的 YAML / Markdown 文件驱动。**日常更新（加人、加新闻、加成果）只需要改这些文件、保存、刷新浏览器，不需要碰 HTML/CSS/JS。**

---

## 1. 新增 / 修改成员 —— `contents/people.yml`

在对应分组（`Group Leader` / `Faculty Members` / `Ph.D. Students` / `Master Students` / `alumni`）的 `members` 列表下加一条：

```yaml
- name: Xinyu Wang 王欣宇        # 必填
  title: Lecturer                # 可选，职称
  subtitle: TAGMA Group Leader   # 可选，第二行说明
  period: M.S. Student (09/2026–) # 可选，入学/在职时间
  supervisor: Supervised by Prof. Jie Wang  # 可选，导师这行文字自己写全
  research: Graph Machine Learning          # 可选，研究方向
  photo: static/assets/img/wang-xinyu.jpg   # 可选，不填则显示占位图标
  homepage: http://127.0.0.1:8080/index.html  # 可选，个人主页链接
  links:                          # 可选，联系方式
    email: xinyu@example.com
    github: https://github.com/xxx
    scholar: https://scholar.google.com/citations?user=xxx
```

- 只有 `name` 必填，其余字段不填就不显示对应内容/图标。
- 照片放进 `static/assets/img/` 文件夹，文件名和 `photo:` 路径对上即可；想要跟其他人统一的头像裁剪风格（居中人脸、露肩），把照片发给我，用人脸检测脚本处理一下就行。
- 顶部 `showAcademicAdvisors: true/false` 控制是否显示 Academic Advisors 板块（8081 是方案 B/含，8082 是方案 A/不含，两边需要分别改，不会自动同步）。

---

## 2. 新增新闻 —— `contents/news.yml`

在 `items` 下加一条：

```yaml
- date: "2026.10"
  text: 某某同学的论文被 CVPR 2027 接收。
  image: static/assets/img/news-2026-10.jpg   # 可选，不填就是纯文字卡片
```

- 按 `date` 倒序自动排列，年份变化会自动加年份小标题。
- 左侧图片轮播、右侧文字列表用的是同一份数据；右侧列表的翻页是**按左侧图片栏的实际高度自动分页**的，加多少条都不用手动控制每页几条。

---

## 3. 新增成果（论文/专利）—— `contents/achievements.yml`

```yaml
publications:
  - year: 2027
    text: >-
      Zuo San, Jie Wang†. Paper Title. Conference/Journal, 2027.
patents:
  - year: 2027
    text: 专利名称，发明专利，申请号 XXXXXXXX。
```

- 每类（`publications`/`patents`）里年份最新的那一条会自动带闪动的 "New" 标记，不用手动加。

---

## 4. 改首页文字介绍 —— `contents/home.md` 和 `contents/config.yml`

- `home.md`：首页 About 段落，Markdown 格式，改完直接生效。
- `config.yml`：标题、导航栏文字、页头大标语（`top-section-bg-text`）等站点级文字。

---

## 5. 本地预览

- 8081 端口 = 小组页面（方案 B，含 Academic Advisors）
- 8082 端口 = 小组页面（方案 A，不含 Academic Advisors）—— 需要单独同步维护
- 8080 端口 = 程玉威个人主页（示例/模板）

本地用的是免缓存开发服务器（`code/serve_nocache.py`），改完文件**直接刷新**（F5）就能看到最新效果，不需要强制刷新（Ctrl+Shift+R）。如果发现改了文件但页面没变化，先确认服务器是不是这个脚本起的，不是的话可能是浏览器缓存导致的，强刷一下即可。

---

## 6. 什么情况需要改代码（找我）

以下这些超出"填数据"范围，需要改 HTML/CSS/JS，得找我：
- 新增一个板块（比如再加一个"荣誉墙"模块）
- 改版式/配色/动效（比如卡片布局、悬浮效果、动画）
- 新增一种从未有过的字段类型（比如给成员加"视频介绍"）

日常"加人、加新闻、加论文"这类内容更新，改上面提到的 YAML 文件就够了。
